export enum SHOP_SHOW_TYPES {
    GRID = 'grid',
    LIST = 'list',
    SINGLE = 'single',
}

export enum LOCALSTORAGE_KEYS {
    LAST_ROUTE_BEFORE_LOGIN = 'LAST_ROUTE_BEFORE_LOGIN',
}

export enum MODAL_STATE {
    SHOWN_CREATE = 'SHOWN_CREATE',
    SHOWN_UPDATE = 'SHOWN_UPDATE',
    HIDDEN = 'HIDDEN',
}
